# V2.26.2 Test Report

Validated in the release package:

- final patch runs after Early Runner Core and Independent Council patches;
- PAPER daily-loss portfolio veto removed;
- PAPER max-open-position veto removed;
- PAPER total-exposure veto removed;
- PAPER chain-exposure veto removed;
- PAPER maxPositionPct cannot be driven to zero by old exposure math when no hard token block exists;
- paper wallet compatibility fields default to non-binding values;
- autonomous scanner must call `runIndependentCouncil`;
- autonomous scanner throws if an independent Council trace is absent;
- hard token-safety checks remain;
- actual paper-wallet cash remains the capital boundary;
- build-helper Node syntax passes;
- one-folder ZIP integrity passes.
