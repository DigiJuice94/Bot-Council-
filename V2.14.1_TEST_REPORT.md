# V2.14.1 Test Report

## Package validation

- V2.14 cumulative files retained.
- V2.14.1 CSS overrides load after the base dashboard CSS through the existing `v214.css` import.
- Council stage override forces a pure white background.
- Current Decision card outer border is removed and shadow retained.
- Card top offset, width, padding, stat height and action-button height are reduced/tightened.
- Responsive override included for <=720px layouts.

## Concurrency verification

Current paper-wallet / risk configuration supports multiple simultaneous open positions. Default `PAPER_MAX_OPEN_POSITIONS` remains 8. Duplicate open positions for the same token remain intentionally blocked because Guardian scales an existing position rather than opening duplicate positions.
