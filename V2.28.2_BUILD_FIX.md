# Bot War Room V2.28.2 — Reset Button Build Fix

Railway failed with:

`[v2.28.1-paper-reset] Could not find anchor: reset button in paper wallet strip`

Cause:
V2.28 adds `All-time high` to the paper wallet strip before the V2.28.1 reset patch runs.
The reset patch was still exact-matching the older strip that ended immediately after
`Open positions`.

Fix:
The button is now inserted structurally inside `<div className="paper-wallet-strip">`
and no longer depends on which wallet stats are already present.

No strategy or wallet-reset behavior changed.
