# V2.14 Test Report — Runner Genome + Filing Cabinet

## Static validation

All V2.14 TypeScript/TSX release files were passed through the TypeScript 5.8.3 transpiler with diagnostics enabled:

- `autopilot.ts`
- `runner-research.ts`
- `live-gate.ts`
- `paper-wallet.ts`
- `provider-waterfall.ts`
- `route-feasibility.ts`
- `risk.ts`
- `RunnerResearchPanel.tsx`
- `DiagnosticsPanel.tsx`
- `page.tsx`
- `layout.tsx`

Result: **no syntax diagnostics**.

The changed server modules (`autopilot.ts`, `runner-research.ts`, `live-gate.ts`, `paper-wallet.ts`) were also compiled under TypeScript `strict=true` against interface-compatible stubs for the existing V2.13 modules. Result: **passed**.

`prepare-structure.mjs` passed `node --check`. `update-manifest.json` parsed successfully as JSON.

## Release-structure test

`prepare-structure.mjs` was executed against a clean simulated release root. It successfully created and mapped:

- `app/page.tsx`
- `app/layout.tsx`
- `app/v214.css`
- `components/RunnerResearchPanel.tsx`
- `lib/runner-research.ts`
- `lib/live-gate.ts`
- `lib/paper-wallet.ts`
- the cumulative V2.13 provider/risk/autopilot files

Result: **passed**.

## Paper-bankroll reset test

An in-memory runtime test started a `$1,000` paper wallet, applied a `$1,000` paper BUY with no open managed positions, verified cash reached `$0`, and then invoked the V2.14 research-bankroll guard.

Observed result:

- starting cash: `$1,000`
- depleted cash: `$0`
- reset performed: `true`
- restarted cash: `$1,000`
- reset count: `1`
- research capital injected: `$1,000`

Result: **passed**.

## Runner / dumper case-label test

A synthetic in-memory research test created two fresh `$15K` market-cap case files:

- case A later reached `$320K` market cap → labeled `runner`
- case B was reviewed after 30+ minutes at `$5K` market cap → labeled `dumper`

The Filing Cabinet snapshot reported exactly 2 cases, 1 runner and 1 dumper. Code Deciphered remained `false`, confirming that tiny samples cannot unlock the live gate.

Result: **passed**.

## Live-unlock gate test

The live dispatcher was tested in two non-network states:

1. Code Deciphered = false → no live attempt.
2. Code Deciphered = true but no dedicated live executor configured → no live attempt; state reports that the executor is required.

Result: **passed**.

## Not claimed

A full Next.js production build was **not** run in this artifact environment because the current full V2.13 repository/dependency tree is not mounted here. Railway remains the authoritative full-project build after the overlay is applied.

No real-money transaction was sent during validation.
