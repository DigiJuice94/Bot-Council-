# Bot War Room V2.29.1 — Trajectory Type Fix

Railway compiled the application and then TypeScript found two V2.29 typing issues.

## 1. RunnerGenomeGuidance fallback

V2.29 added required Trajectory Observer fields to `RunnerGenomeGuidance`, but the
heuristic fallback object in `lib/engine.ts` still returned the pre-V2.29 shape.

V2.29.1 adds safe fallback values:

- trajectoryScore: 50
- trajectoryDumperRiskScore: 50
- trajectoryConfidence: 0
- trajectoryPhase: INSUFFICIENT
- trajectorySampleSize: 0
- trajectoryChainSampleSize: 0
- trajectoryEvidence: fallback explanation

## 2. trajectoryOutcome union

A trajectory case can internally be `open`, `neutral`, `runner`, or `dumper`,
while private entity memory intentionally accepts only finalized `runner` or
`dumper` outcomes.

V2.29.1 explicitly narrows the value before storing it.

No trading strategy, Council behavior, Exit Strategist logic, paper reset,
Trajectory Observer learning logic, or portfolio history behavior changed.
