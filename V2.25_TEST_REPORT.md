# V2.25 Test Report

Validation completed on the cumulative release package:
- all seven build/patch scripts pass `node --check`;
- `runner-research.ts`, `WarRoomDashboard.tsx`, and `RunnerResearchPanel.tsx` pass TypeScript/TSX parser-level syntax checks;
- CSS brace and malformed literal-newline checks pass;
- `prepare-structure.mjs` applies `early-runner-core-patch.mjs` last, after the older cumulative patches, so V2.25 intentionally overrides the earlier fixed-$50 and mature-liquidity behavior;
- the release ZIP is verified to contain one top-level folder only.

Behavioral implementation checks:
- fresh provider discovery is merged rather than first-provider-wins;
- $10K-$50K market cap gets the highest discovery/model priority;
- Runner Genome is loaded before every Council decision and persisted into the position entry context;
- historical and live Genome feature vectors are aligned;
- the first research observation is preserved permanently even after the rolling observation buffer fills;
- actual peak timestamp is preserved for time-to-peak learning;
- early-runner paper execution ignores the legacy `meme_unsafe` thin-liquidity label while retaining deterministic hard safety checks;
- early-runner execution gets a non-zero execution multiplier even if the old meme classifier's starter multiplier is zero;
- early-runner pool minimum defaults to $1K, with the actual order still required to pass route/slippage simulation;
- approved PAPER entries have a $50 minimum and learned size up to $150;
- WATCH opportunities no longer repeat old Alpha/conviction/quorum filters;
- winner adds remain $50+ but respect the per-position gross winner cap;
- early-runner exit plan no longer uses a generic $15K liquidity floor;
- Guardian queries Exit Genome on every live position mark;
- completed trades persist MFE, MAE, profit capture, hold time and exit reason for exit-learning;
- Entry/Exit Genome stats and $50+ training performance are visible in the Filing Cabinet;
- seven-chain experiment metadata includes HyperEVM;
- live-money graduation/executor gates are unchanged.

Integration limitation: the one-folder upload package is an overlay-style cumulative release and does not contain the repository's complete generated dependency tree before `prepare-structure.mjs` runs. A full isolated Next.js production build therefore cannot be truthfully reproduced inside this artifact container. Railway's build against the repository remains the final integration test after upload.
