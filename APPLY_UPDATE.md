# Apply Bot War Room V2.22

This is a cumulative one-folder update.

Upload/replace the contents of `Bot-War-Room-V2.22-FULL` into the repository root and redeploy Railway.

No new environment variables are required; the $50 paper-training minimum is the default.

Optional overrides:
- `PAPER_MIN_BUY_USD`
- `PAPER_MIN_SCALE_IN_USD`
- `PAPER_EXPLORATION_MAX_PCT`
- `PAPER_AUTO_REFILL_THRESHOLD_USD`
