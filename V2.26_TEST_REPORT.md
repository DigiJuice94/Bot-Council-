# V2.26 Test Report

Validated in the release package:

- `agent-entity-runtime.ts` defines seven separate specialist entity identities
- each private specialist call is issued independently and in parallel
- private calls receive no peer outputs
- private outputs lock before meeting calls begin
- seven meeting calls occur after reveal and can independently change votes
- Runner CIO is a separate eighth call
- deterministic Executor is outside the eight Council seats
- Portfolio Strategist replaces Executor as the seventh specialist seat
- per-entity Redis memory keys are separate
- closed position outcomes are written back to each entity's own memory
- PositionEntryContext stores the independent Council trace with the trade
- audit trail distinguishes private round / meeting round / CIO / deterministic Executor
- dashboard roster uses eight entities and shows proof strip
- missing `OPENAI_API_KEY` does not silently masquerade as true independent AI
- V2.25 Early Runner Core remains ordered before the V2.26 independent-Council patch
- Node patch syntax passes
- new TypeScript source parser syntax passes
- changed TSX parser syntax passes
- CSS structural checks pass
- one-folder ZIP integrity passes

A full Next.js production build was not claimed in this artifact environment because the cumulative upload package still relies on the existing repository's flat source files that `prepare-structure.mjs` restores during Railway build.
