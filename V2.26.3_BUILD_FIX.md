# Bot War Room V2.26.3 — Railway Structural Patch Fix

Railway failed with:

`[v2.25-early-runner-core] Could not find structural boundary: WATCH becomes active training opportunity`

The previous build-hardening code had one escaping mistake. It searched for the literal characters `\n` inside generated TypeScript instead of actual line breaks.

V2.26.3 corrects all three structural newline paths introduced in V2.26.1:

- dynamic $50+ Runner Genome entry sizing
- WATCH -> active PAPER training opportunity
- winner scale-in sizing

This is a build-pipeline correction only.

It does NOT restore any paper kill switch.
It does NOT remove the independent eight-entity Council.
It does NOT change the Early Runner mission.
