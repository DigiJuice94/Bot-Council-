# Bot War Room V2.14.3 — Direct Council Layout Fix

This patch replaces the missing `WarRoomDashboard.tsx` source so the council scene and the paper-trader panel can be controlled directly.

## Fixes included
- removes the lingering horizontal split feeling in the council scene by replacing the scene container
- keeps the full council area on one continuous white background
- places the real-data autonomous paper trader block below the last bot chair
- preserves a higher, rectangular current-decision card with soft shadow
- keeps the clean dashboard panels underneath
- keeps the multi-position paper trading flow and the Runner Genome / Filing Cabinet updates intact

## Important
Earlier layout-only patches could not affect the live page because the actual `WarRoomDashboard.tsx` file was not present in the overlay. This patch includes that file directly.
