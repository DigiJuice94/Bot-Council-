# V2.11 Validation Report

## Council art regression
- The rendered dashboard imports `COUNCIL_ART_DATA_URI` instead of relying on a `/public` URL.
- The embedded PNG bytes exactly match `bot-council-reference.png`.
- SHA-256: `75e8da00352cd7ebd00602251ce19aaa39d24ae23d9814374247257c85a4836c`.
- This removes the V2.10.1 failure mode where the CSS stand-ins were hidden while the public image failed to resolve.

## Autonomous behavior
- `instrumentation.ts` starts the autonomous worker at Node server boot.
- `/api/autopilot` also calls the same idempotent starter, so status polling cannot create duplicate loops.
- The worker rotates through all six configured chains without a chain-selection click.
- Approved paper BUYs are executed server-side and registered with Position Guardian automatically.
- Existing duplicate-position, cooldown, re-entry, hard-risk and Guardian rules remain in the execution path.
- Demo fallback candidates are not used by the autonomous executor. If `MARKET_DATA_BASE_URL` is missing, the worker waits instead of manufacturing trades.

## UI regression
- `Scan next candidate` control removed.
- `Auto scout every 8s` toggle removed.
- Manual `Execute paper` button removed.
- Chat Log, Trades Log and eight-role Bot Roster added beneath the council scene.
- Existing V2.10 Launch Velocity, V2.9 Council quorum and V2.8 Winner Engine files remain in the package.

## Static validation
- `node --check prepare-structure.mjs`: passed.
- Core TypeScript compilation including `autopilot.ts`: passed with a local Redis/Node declaration stub used only for this isolated update-folder test.
- Embedded-art byte equality assertion: passed.

A full Next.js production build was not run in the update-only folder because it does not contain the repository's installed `node_modules`. Railway/the full repo should run `npm run build`, which reconstructs the structured paths before Next builds.
