# V2.8 Local Validation Report

Validation performed on the update package before packaging.

## Static validation
- Core TypeScript modules compiled with TypeScript 5.8.3 using local stubs for Redis runtime types.
- `prepare-structure.mjs` passed Node syntax validation.
- `package.json` and `update-manifest.json` parsed successfully.
- TSX/API files produced no TypeScript syntax-class errors in a no-resolve parse pass; unresolved React/Next aliases are expected in this update-only folder without the full repository/node_modules.

## Winner Engine targeted runtime test
A deliberately strong Solana meme snapshot was replayed through the actual V2.8 engine:
- Decision: BUY
- Alpha: 86/100
- Blended conviction: 87%
- Regime: Meme Expansion
- Initial paper notional on the default $10,000 context: $180
- Moonbag target: 20%
- Staged take-profit distribution: 80%

The same position was then marked +6% with stronger flow/volume confirmation:
- Guardian action: `SCALE_IN`
- Pending stage: `ADD1`
- Confirmation score: ~90/100

The same entry was marked below entry:
- Guardian did **not** produce `SCALE_IN`.
- This verifies the explicit no-average-down rule in the targeted path.

## Benchmark sanity check
A 220-frame synthetic profitability benchmark completed with finite metrics and emitted the V2.8 warning that dynamic pyramiding is not silently simulated from price-only paths. Pyramiding requires a full event-driven historical replay with contemporaneous snapshots at each potential add point.

## Limitations
This report is a code/behavior validation, not proof of future profitability. The next meaningful V2.8 test is a timestamped historical event replay that can evaluate ADD1/ADD2/ADD3, partial exits, trails, re-entry, and moonbag behavior in correct chronological order.
