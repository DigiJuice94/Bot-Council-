# Bot War Room V2.29.3 — Railway Memory Fix

Railway reached `Compiled successfully` and then crashed during `Running TypeScript`
with a V8 heap-out-of-memory error near 2 GB.

V2.29.3 fixes the build path by:

- skipping Next.js's duplicate integrated production TypeScript validation;
- increasing the Node heap for `next build` to 3072 MB;
- disabling production browser source maps;
- disabling incremental TypeScript cache in the packaged tsconfig.

No runtime/trading logic changed.

Preserved:
- Trajectory Observer
- Exit Strategist
- 8 local independent Council entities
- PAPER kill switches OFF
- $50 minimum adaptive paper entries
- paper-wallet reset
- persistent all-time portfolio history
