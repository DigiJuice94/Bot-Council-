# V2.27 Test Report

Validated:
- no `api.openai.com` reference remains in the Council runtime;
- no `OPENAI_API_KEY` is required by the Council runtime;
- seven specialist private reads run via Promise.all before peer reveal;
- each private read loads only that entity's private memory;
- meeting round happens only after private reads complete;
- Runner CIO is a separate eighth local entity;
- dashboard recognizes `independent-local` as verified;
- repeated autonomous errors are rate-limited in chat;
- PAPER kill-switch-removal patch remains final before V2.27 resilience;
- build helper syntax passes;
- TypeScript parser-level syntax for the new runtime passes;
- one-folder ZIP integrity passes.

This release intentionally removes paid LLM inference from the Council. These are independent local decision entities, not language-model instances.
