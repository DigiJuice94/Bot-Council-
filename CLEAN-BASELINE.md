# Clean baseline — V3.6.3.13

One complete project folder with one `deployment-source.tar.gz` holding the same `app/`, `components/`, `lib/` and `public/` tree. Unverified exits retain their remaining cost as at-risk capital without a sale or loss; fresh observed liquidity permits explicitly modeled PAPER exits. Confirmed security/zero-liquidity failures remain losses. See `CHANGELOG-V3.6.3.13.md`.
