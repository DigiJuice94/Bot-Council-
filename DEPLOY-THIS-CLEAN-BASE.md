# Deploy this complete folder

Put this folder's **contents** at the repository root, including `app/`, `components/`, `lib/`, `package.json`, `Dockerfile`, and `deployment-source.tar.gz`. Docker restores the archive before building. The live dashboard must show `Bot War Room V3.6.3.13`. Do not reset the paper wallet: the accounting repair is derived from existing persisted positions.
