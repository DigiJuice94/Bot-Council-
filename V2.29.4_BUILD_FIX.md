# Bot War Room V2.29.4 — Standalone Build Fix

Railway's Docker runner stage copies:

`/app/.next/standalone`

V2.29.3 successfully changed the Next build memory behavior, but its new
`next.config.mjs` did not set `output: "standalone"`. That meant Next never
generated `.next/standalone`, so the Docker COPY step failed.

V2.29.4 adds:

`output: "standalone"`

No trading, Council, Trajectory Observer, Exit Strategist, wallet reset,
portfolio history, or paper strategy behavior changed.
