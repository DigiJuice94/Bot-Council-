// V2.11.2: production discovery is real-data only.
// This legacy module remains only so older imports fail toward the live provider instead of demo fixtures.
export { fetchLiveCandidate } from "./market-data";
